# .env.local — copia este archivo y llena tus datos
# NUNCA subas este archivo a git

# Base de datos Vicidial
DB_HOST=192.168.1.100
DB_PORT=3306
DB_USER=cron
DB_PASS=cron_pass
DB_NAME=asterisk

# JWT — mínimo 32 caracteres aleatorios, cámbialo en producción
JWT_SECRET=super_secreto_largo_y_aleatorio_cambiame_ya
JWT_EXPIRES=8h
